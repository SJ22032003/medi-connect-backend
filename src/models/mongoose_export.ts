import mongoose, { Document, Schema, Error, Collection, Model } from "mongoose";
import { MongoServerError, MongoClient, MongoClientOptions } from "mongodb";

export {
  mongoose,
  Document,
  Schema,
  MongoServerError,
  Error,
  Collection,
  MongoClient,
  MongoClientOptions,
  Model,
};
